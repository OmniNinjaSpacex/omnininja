name: Production smoke test (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Explain manual-only status
        run: |
          echo "Automatic smoke testing is paused until OmniNinja has a new public production URL."
          echo "The previous chatgpt.site address returned HTTP 401 and is intentionally not tested on every push."
